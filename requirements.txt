# Core numerical / data handling
numpy>=1.24
pandas>=2.0
scipy>=1.10
openpyxl>=3.1        # required by pandas to read the .xlsx sensor logs

# Machine learning
tensorflow>=2.13     # Keras models (.keras) for the grasp classifiers
scikit-learn>=1.3    # StandardScaler, train/test splitting, metrics
joblib>=1.3           # saved/loaded scalers (*.joblib)

# Plotting (training curves, confusion matrices, RRT* visualization)
matplotlib>=3.7
